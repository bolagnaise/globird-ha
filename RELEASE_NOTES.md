## v0.1.42

- Correctly classify Solar Soak and other solar-named shoulder import rates as usage instead of solar export. Export remains identified from `B*` meter registers or explicit export/feed-in metadata, so recent solar export sensors no longer include Solar Soak consumption.
